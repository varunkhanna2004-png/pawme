# Secrets — ignore every .env variant (incl. editor backups like .env.save)
.env
.env.*
!.env.example
.env.local
.env.*.local

# Dependencies / build output
node_modules/
dist/
.vercel/

# e2e output
e2e/shots/
