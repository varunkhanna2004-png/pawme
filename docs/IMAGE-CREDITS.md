# Image credits

Every image in PAWME is **original vector artwork drawn in code** for this project. No stock photos, no photos scraped from the web, no third-party illustrations.

| Asset | Source | Licence |
|---|---|---|
| Welcome-screen hero (dog, cat, heart, paw prints) | `src/components/PetArt.tsx` — original SVG | © PAWME, all rights reserved |
| Link-preview card `public/og.png` (1200×630) | generated by `scripts/build-og.mjs` (`npm run og`) from the same original shapes | © PAWME |
| App icons `public/icon-*.png`, `apple-touch-icon.png` | generated paw mark (see `vite.config.ts` history) | © PAWME |
| Seed pet photos (dev only) | drawn by `scripts/seed.mjs`, each stamped SEED | © PAWME; never in production |
| Makati boundary polygon (`supabase/migrations/…cluster_makati.sql`) | OpenStreetMap relation 103716 | © OpenStreetMap contributors, ODbL 1.0 — data, not imagery |

Fonts: the OG image is rendered with DejaVu Sans (Bitstream Vera licence, free to embed); the app uses the device's system font.

If real pet photography is ever added, record its source and licence here before it ships.
